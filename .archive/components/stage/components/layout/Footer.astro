---
const bottomLinks = [
  {
    header: 'Links',
    links: [
      {
        name: 'Status',
        href: 'https://status.fryrvo.com',
        isExternal: true,
      },
      {
        name: 'Documentation',
        href: 'https://docs.fryrvo.com',
        isExternal: true,
      },
      {
        name: 'Support',
        href: 'https://support.fryrvo.com',
        isExternal: true,
      },
    ],
  },
  {
    header: 'Legal',
    links: [
      {
        name: 'Privacy',
        href: '/stage/privacy',
        isExternal: false,
      },
      {
        name: 'Terms of Service',
        href: '/stage/tos',
        isExternal: false,
      },
      {
        name: 'Credits',
        href: '/stage/credit',
        isExternal: false,
      },
    ],
  },
  {
    header: 'STAGE',
    links: [
      {
        name: 'STAGE 1',
        href: '/stage/404',
        isExternal: false,
      },
      {
        name: 'STAGE 2',
        href: '/stage/404',
        isExternal: false,
      },
      {
        name: 'STAGE 3',
        href: '/stage/404',
        isExternal: false,
      },
    ],
  },
  {
    header: 'STAGE 2',
    links: [
      {
        name: 'STAGE 1',
        href: '/stage/test/video',
        isExternal: false,
      },
      {
        name: 'STAGE 2',
        href: '/stage/blog',
        isExternal: false,
      },
      {
        name: 'STAGE 3',
        href: '/stage/404',
        isExternal: false,
      },
    ],
  },
];
---

<footer
  class="bg-primary-500/10 dark:bg-primary-400/10 mt-16 py-16 lg:mt-20 lg:py-20"
  aria-labelledby="footer-heading"
>
  <h2 id="footer-heading" class="sr-only">Footer</h2>


  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
    <div class="flex flex-col justify-between gap-12 sm:gap-16 lg:flex-row">

<!-- Branding -->
<div class="flex max-w-sm flex-col items-start gap-3 sm:gap-4">
  <img
    id="site-logo-stage-foot"
    class="h-10 w-auto cursor-pointer transition-transform duration-100 active:scale-90"
    width="100"
    height="70"
    src="https://cdn.fryrvo.com/assets/fryrvo_branding/STAGE_Logo.png"
    alt="Fryrvo"
  />

  <script>
    let c = 0, t: any;

    document.addEventListener('click', (e) => {
      const target = e.target as HTMLElement;
      if (target && target.id === 'site-logo-stage-foot') {
        clearTimeout(t);
        if (++c >= 7) return window.location.href = "/";
        t = setTimeout(() => c = 0, 1500);
      }
    });






const secretWord = "stage";
          let typedBuffer = "";

          document.addEventListener("keydown", (e) => {
            if (e.key.length > 1) return;

            typedBuffer += e.key.toLowerCase();
            
            if (typedBuffer.length > secretWord.length) {
              typedBuffer = typedBuffer.slice(-secretWord.length);
            }

            if (typedBuffer === secretWord) {
              document.body.style.transition = 'transform 1s ease';
              document.body.style.transform = 'rotate(360deg)';
              setTimeout(() => {
                document.body.style.transform = 'none';
              }, 1000);
              typedBuffer = "";
            }
          });

(() => {
  const pingWord = "ping";
  let pingBuf = "";

  const pingToast = document.createElement("div");
  pingToast.textContent = "Pong!";
  pingToast.style.position = "fixed";
  pingToast.style.bottom = "-50px";
  pingToast.style.left = "50%";
  pingToast.style.transform = "translateX(-50%)";
  pingToast.style.backgroundColor = "rgba(14, 165, 233, 0.9)";
  pingToast.style.color = "#fff";
  pingToast.style.padding = "10px 20px";
  pingToast.style.borderRadius = "9999px";
  pingToast.style.fontSize = "14px";
  pingToast.style.fontWeight = "600";
  pingToast.style.boxShadow = "0 10px 15px -3px rgba(0, 0, 0, 0.3)";
  pingToast.style.transition = "bottom 0.5s ease, opacity 0.5s ease";
  pingToast.style.opacity = "0";
  pingToast.style.zIndex = "9999";
  document.body.appendChild(pingToast);

  window.addEventListener("keydown", (e) => {
    if (e.target.tagName === "INPUT" || e.target.tagName === "TEXTAREA" || e.target.isContentEditable) return;
    if (e.key.length !== 1) return;
    
    const char = e.key.toLowerCase();
    pingBuf += char;

    if (pingBuf.length > pingWord.length) {
      pingBuf = pingBuf.slice(-pingWord.length);
    }

    if (pingBuf === pingWord) {
      pingToast.style.bottom = "40px";
      pingToast.style.opacity = "1";
      setTimeout(() => {
        pingToast.style.bottom = "-50px";
        pingToast.style.opacity = "0";
      }, 2000);
      pingBuf = "";
    }
  });
})();

(() => {
  const transWord = "trans";
  let transBuf = "";

  const transToast = document.createElement("div");
  transToast.style.position = "fixed";
  transToast.style.bottom = "-80px";
  transToast.style.left = "0";
  transToast.style.width = "100%";
  transToast.style.textAlign = "center";
  transToast.style.padding = "16px 0";
  transToast.style.fontSize = "18px";
  transToast.style.fontWeight = "800";
  transToast.style.letterSpacing = "1px";
  transToast.style.boxShadow = "0 -4px 20px rgba(0, 0, 0, 0.2)";
  transToast.style.transition = "bottom 0.5s ease, opacity 0.5s ease";
  transToast.style.opacity = "0";
  transToast.style.zIndex = "9999";
  document.body.appendChild(transToast);

  window.addEventListener("keydown", (e) => {
    if (e.target.tagName === "INPUT" || e.target.tagName === "TEXTAREA" || e.target.isContentEditable) return;
    if (e.key.length !== 1) return;
    
    const char = e.key.toLowerCase();
    transBuf += char;

    if (transBuf.length > transWord.length) {
      transBuf = transBuf.slice(-transWord.length);
    }

    if (transBuf === transWord) {
      
      const isHumanRight = Math.random() < 0.7;

      if (isHumanRight) {
        transToast.textContent = "Fryrvo Love human Right";
        transToast.style.background = "linear-gradient(90deg, #5BCEFA, #F5A9B8, #FFFFFF, #F5A9B8, #5BCEFA)";
        transToast.style.color = "#111111";
        transToast.style.borderTop = "3px solid #5BCEFA";
        transToast.style.borderBottom = "3px solid #F5A9B8";
      } else {
        transToast.textContent = "Cath is non-binary! Now you know for sure";
        transToast.style.background = "linear-gradient(90deg, #FCF434, #FFFFFF, #9C59D1, #2C2C2C)";
        transToast.style.color = "#111111";
        transToast.style.borderTop = "3px solid #FCF434";
        transToast.style.borderBottom = "3px solid #2C2C2C";
      }
        
      // hey if you are reading this well um don't leak me
      // anyway no one should hide who they are isn't it?
      transToast.style.bottom = "0";
      transToast.style.opacity = "1";
      setTimeout(() => {
        transToast.style.bottom = "-80px";
        transToast.style.opacity = "0";
      }, 3000);
      transBuf = "";
    }
  });
})();


          const originalTitle = document.title;
          const awayMessages = [
            "Hey, come back! 😭",
            "Did you get distracted by YouTube?",
            "We miss you over here! 👀",
            "try type stage "
          ];

          document.addEventListener('visibilitychange', () => {
            if (document.hidden) {
              const randomMessage = awayMessages[Math.floor(Math.random() * awayMessages.length)];
              document.title = randomMessage;
            } else {
              document.title = originalTitle;
            }
          });


          (() => {
  let pingBuf = "";

  const pingToast = document.createElement("div");
  pingToast.textContent = "forgot to change tab isn't it silly you";
  pingToast.style.position = "fixed";
  pingToast.style.bottom = "-50px";
  pingToast.style.left = "50%";
  pingToast.style.transform = "translateX(-50%)";
  pingToast.style.backgroundColor = "rgba(14, 165, 233, 0.9)";
  pingToast.style.color = "#fff";
  pingToast.style.padding = "10px 20px";
  pingToast.style.borderRadius = "9999px";
  pingToast.style.fontSize = "14px";
  pingToast.style.fontWeight = "600";
  pingToast.style.boxShadow = "0 10px 15px -3px rgba(0, 0, 0, 0.3)";
  pingToast.style.transition = "bottom 0.5s ease, opacity 0.5s ease";
  pingToast.style.opacity = "0";
  pingToast.style.zIndex = "9999";
  document.body.appendChild(pingToast);

  window.addEventListener("keydown", (e) => {
    if (e.target.tagName === "INPUT" || e.target.tagName === "TEXTAREA" || e.target.isContentEditable) return;
    if (e.key.length !== 1) return;
    
    const char = e.key.toLowerCase();
    pingBuf += char;

    if (pingBuf.length > 5) {
      pingBuf = pingBuf.slice(-5);
    }

    if (pingBuf.endsWith("google") || pingBuf.endsWith("sudo") || pingBuf.endsWith("git") || pingBuf.endsWith("cd")) {
      pingToast.style.bottom = "40px";
      pingToast.style.opacity = "1";
      setTimeout(() => {
        pingToast.style.bottom = "-50px";
        pingToast.style.opacity = "0";
      }, 2000);
      pingBuf = "";
    }
  });
})();

(() => {
  let pingBuf = "";

  const pingToast = document.createElement("div");
  pingToast.textContent = "boink";
  pingToast.style.position = "fixed";
  pingToast.style.bottom = "-50px";
  pingToast.style.left = "50%";
  pingToast.style.transform = "translateX(-50%)";
  pingToast.style.backgroundColor = "rgba(14, 165, 233, 0.9)";
  pingToast.style.color = "#fff";
  pingToast.style.padding = "10px 20px";
  pingToast.style.borderRadius = "9999px";
  pingToast.style.fontSize = "14px";
  pingToast.style.fontWeight = "600";
  pingToast.style.boxShadow = "0 10px 15px -3px rgba(0, 0, 0, 0.3)";
  pingToast.style.transition = "bottom 0.5s ease, opacity 0.5s ease";
  pingToast.style.opacity = "0";
  pingToast.style.zIndex = "9999";
  document.body.appendChild(pingToast);

  window.addEventListener("keydown", (e) => {
    if (e.target.tagName === "INPUT" || e.target.tagName === "TEXTAREA" || e.target.isContentEditable) return;
    if (e.key.length !== 1) return;
    
    const char = e.key.toLowerCase();
    pingBuf += char;

    if (pingBuf.length > 5) {
      pingBuf = pingBuf.slice(-5);
    }

    if (pingBuf.endsWith("b") || pingBuf.endsWith("j")) {
      pingToast.style.bottom = "40px";
      pingToast.style.opacity = "1";
      setTimeout(() => {
        pingToast.style.bottom = "-50px";
        pingToast.style.opacity = "0";
      }, 2000);
      pingBuf = "";
    }
  });
})();

(() => {
  let pingBuf = "";

  const pingToast = document.createElement("div");
  pingToast.textContent = "aw don't be too sad oh i think i should said to myself silly me";
  pingToast.style.position = "fixed";
  pingToast.style.bottom = "-50px";
  pingToast.style.left = "50%";
  pingToast.style.transform = "translateX(-50%)";
  pingToast.style.backgroundColor = "rgba(14, 165, 233, 0.9)";
  pingToast.style.color = "#fff";
  pingToast.style.padding = "10px 20px";
  pingToast.style.borderRadius = "9999px";
  pingToast.style.fontSize = "14px";
  pingToast.style.fontWeight = "600";
  pingToast.style.boxShadow = "0 10px 15px -3px rgba(0, 0, 0, 0.3)";
  pingToast.style.transition = "bottom 0.5s ease, opacity 0.5s ease";
  pingToast.style.opacity = "0";
  pingToast.style.zIndex = "9999";
  document.body.appendChild(pingToast);

  window.addEventListener("keydown", (e) => {
    if (e.target.tagName === "INPUT" || e.target.tagName === "TEXTAREA" || e.target.isContentEditable) return;
    if (e.key.length !== 1) return;
    
    const char = e.key.toLowerCase();
    pingBuf += char;

    if (pingBuf.length > 5) {
      pingBuf = pingBuf.slice(-5);
    }

    if (pingBuf.endsWith("sad") || pingBuf.endsWith("bad") || pingBuf.endsWith("help") || pingBuf.endsWith("why")) {
      pingToast.style.bottom = "40px";
      pingToast.style.opacity = "1";
      setTimeout(() => {
        pingToast.style.bottom = "-50px";
        pingToast.style.opacity = "0";
      }, 2000);
      pingBuf = "";
    }
  });
})();



(() => {
  let pingBuf = "";

  const pingToast = document.createElement("div");
  pingToast.textContent = "don't worries i think i also";
  pingToast.style.position = "fixed";
  pingToast.style.bottom = "-50px";
  pingToast.style.left = "50%";
  pingToast.style.transform = "translateX(-50%)";
  pingToast.style.backgroundColor = "rgba(14, 165, 233, 0.9)";
  pingToast.style.color = "#fff";
  pingToast.style.padding = "10px 20px";
  pingToast.style.borderRadius = "9999px";
  pingToast.style.fontSize = "14px";
  pingToast.style.fontWeight = "600";
  pingToast.style.boxShadow = "0 10px 15px -3px rgba(0, 0, 0, 0.3)";
  pingToast.style.transition = "bottom 0.5s ease, opacity 0.5s ease";
  pingToast.style.opacity = "0";
  pingToast.style.zIndex = "9999";
  document.body.appendChild(pingToast);

  window.addEventListener("keydown", (e) => {
    if (e.target.tagName === "INPUT" || e.target.tagName === "TEXTAREA" || e.target.isContentEditable) return;
    if (e.key.length !== 1) return;
    
    const char = e.key.toLowerCase();
    pingBuf += char;

    if (pingBuf.length > 20) {
      pingBuf = pingBuf.slice(-5);
    }

    if (pingBuf.endsWith("ifyoutypethisrandomllyyouprobneedsomehelpalso") || pingBuf.endsWith("needhelp?")) {
      pingToast.style.bottom = "40px";
      pingToast.style.opacity = "1";
      setTimeout(() => {
        pingToast.style.bottom = "-50px";
        pingToast.style.opacity = "0";
      }, 2000);
      pingBuf = "";
    }
  });
})();


        </script>


        <!-- Social Media Icons tings  -->
        <div class="flex items-center gap-3">
          <a
            href="https://github.com/fryrvoStudio/Site"
            target="_blank"
            rel="noopener noreferrer"
            class="text-primary-950/70 hover:text-primary-950 dark:text-primary-200/70 dark:hover:text-primary-200 transition"
            aria-label="Github"
          >
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.53 1.032 1.53 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"/></svg>
          </a>
          <!-- <a
            href="https://open.spotify.com/artist/6D4MMKyp97IcBtp3U43Xb3"
            target="_blank"
            rel="noopener noreferrer"
            class="text-primary-950/70 hover:text-primary-950 dark:text-primary-200/70 dark:hover:text-primary-200 transition"
            aria-label="Spotify"
          >
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.477 2 2 6.477 2 12c0 5.523 4.477 10 10 10 5.523 0 10-4.477 10-10 0-5.523-4.477-10-10-10zm4.586 14.424c-.18.295-.563.387-.857.207-2.35-1.436-5.308-1.76-8.785-.964-.335.077-.667-.133-.744-.468-.077-.335.133-.667.468-.744 3.79-.868 7.087-.492 9.774 1.147.295.18.387.562.207.822zm1.225-2.724c-.225.366-.703.48-1.069.255-2.693-1.652-6.801-2.131-9.988-1.166-.412.125-.851-.103-.976-.515-.125-.412.103-.851.515-.976 3.619-1.1 8.136-.554 11.238 1.348.366.225.48.703.255 1.054zm.106-2.842C14.773 8.877 8.57 8.665 5.006 9.747c-.502.153-1.033-.122-1.186-.624-.153-.502.122-1.033.624-1.186 4.053-1.233 11.011-.983 15.253 1.536.452.268.602.855.334 1.307-.268.452-.855.602-1.307.334z"/></svg>
          </a> -->

          <!-- <a
            href="https://youtube.com/@fryrvo"
            target="_blank"
            rel="noopener noreferrer"
            class="text-primary-950/70 hover:text-primary-950 dark:text-primary-200/70 dark:hover:text-primary-200 transition"
            aria-label="Youtube"
          >
            <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor"><path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/></svg>
          </a> -->
        
          <!-- <a
            href="https://tech.lgbt/@cath"
            target="_blank"
            rel="noopener noreferrer"
            class="text-primary-950/70 hover:text-primary-950 dark:text-primary-200/70 dark:hover:text-primary-200 transition"
            aria-label="Mastodon"
          >
            <svg role="img" class="h-5 w-5" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" fill="currentColor"><title>Mastodon</title><path d="M23.268 5.313c-.35-2.578-2.617-4.61-5.304-5.004C17.51.242 15.792 0 11.813 0h-.03c-3.98 0-4.835.242-5.288.309C3.882.692 1.496 2.518.917 5.127.64 6.412.61 7.837.661 9.143c.074 1.874.088 3.745.26 5.611.118 1.24.325 2.47.62 3.68.55 2.237 2.777 4.098 4.96 4.857 2.336.792 4.849.923 7.256.38.265-.061.527-.132.786-.213.585-.184 1.27-.39 1.774-.753a.057.057 0 0 0 .023-.043v-1.809a.052.052 0 0 0-.02-.041.053.053 0 0 0-.046-.01 20.282 20.282 0 0 1-4.709.545c-2.73 0-3.463-1.284-3.674-1.818a5.593 5.593 0 0 1-.319-1.433.053.053 0 0 1 .066-.054c1.517.363 3.072.546 4.632.546.376 0 .75 0 1.125-.01 1.57-.044 3.224-.124 4.768-.422.038-.008.077-.015.11-.024 2.435-.464 4.753-1.92 4.989-5.604.008-.145.03-1.52.03-1.67.002-.512.167-3.63-.024-5.545zm-3.748 9.195h-2.561V8.29c0-1.309-.55-1.976-1.67-1.976-1.23 0-1.846.79-1.846 2.35v3.403h-2.546V8.663c0-1.56-.617-2.35-1.848-2.35-1.112 0-1.668.668-1.67 1.977v6.218H4.822V8.102c0-1.31.337-2.35 1.011-3.12.696-.77 1.608-1.164 2.74-1.164 1.311 0 2.302.5 2.962 1.498l.638 1.06.638-1.06c.66-.999 1.65-1.498 2.96-1.498 1.13 0 2.043.395 2.74 1.164.675.77 1.012 1.81 1.012 3.12z"/></svg>
          </a> -->

        </div>

        <p class="text-primary-950/70 dark:text-primary-200/70 text-2xl font-bold tracking-tight">
          STAGE Environment
        </p>

        <p class="text-primary-950/70 dark:text-primary-200/70 text-sm">© 2026 <a href="https://fryrvo.com" class="no-underline hover:underline">Fryrvo</a></p>
      </div>

      <!-- Bottom links -->
      <div class="grid max-w-xl flex-1 grid-cols-1 gap-6 sm:grid-cols-4">
        {
          bottomLinks.map((group) => (
            <div class="flex flex-col gap-3">
              <p class="text-sm font-medium uppercase tracking-wider">{group.header}</p>
              <ul role="list" class="flex flex-col gap-2">
                {group.links.map((link) => (
                  <li>
                    <a
                      href={link.href}
                      class="focus-visible:outline-primary-950 dark:focus-visible:outline-primary-200 inline-flex items-center gap-1 rounded-md text-sm hover:underline focus-visible:outline focus-visible:outline-2"
                      target={link.isExternal ? '_blank' : undefined}
                      rel={link.isExternal ? 'noopener' : undefined}
                    >
                      {link.name}
                      {link.isExternal && (
                        <svg
                          class="h-3.5 w-3.5"
                          xmlns="http://www.w3.org/2000/svg"
                          viewBox="0 0 24 24"
                          fill="currentColor"
                          aria-hidden="true"
                        >
                          <path d="M10 6V8H5V19H16V14H18V20C18 20.5523 17.5523 21 17 21H4C3.44772 21 3 20.5523 3 20V7C3 6.44772 3.44772 6 4 6H10ZM21 3V11H19L18.9999 6.413L11.2071 14.2071L9.79289 12.7929L17.5849 5H13V3H21Z" />
                        </svg>
                      )}
                    </a>
                  </li>
                ))}
              </ul>
            </div>
          ))
        }
      </div>

    </div>
  </div>
</footer>
